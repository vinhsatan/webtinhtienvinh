# 10 — Simulation Harness & Reports

Subtasks:
- Build a harness that can run scenario scripts and produce `simulation/report.json`.
- Add scenario library (happy path + edge-cases + adversarial inputs).
- Add CI job to run core scenarios on every PR.
