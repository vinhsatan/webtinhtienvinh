# 12 — Integration Tests & CI Jobs

Subtasks:
- Add vitest integration pipelines and required dev deps in CI.
- Create DB-backed test harness with ephemeral Postgres via Docker for CI.
- Add canary integration stage and test data teardown hooks.
