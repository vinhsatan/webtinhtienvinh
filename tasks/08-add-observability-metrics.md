# 08 — Add Observability & Metrics

Subtasks:
- Integrate `prom-client` metrics endpoint and counters/histograms.
- Initialize OTel SDK in `src/opentelemetry/init.ts` and add traces.
- Add dashboards + sample Grafana queries (docs snippet).
- Add CI smoke test for `/metrics` endpoint.
