# Example environment for production/single-user deployment
# ---------------------------------------------------------------
# LOCAL DEV QUICK-START:
#   1. Copy this file to .env:  cp .env.example .env
#   2. Set AUTH_EMAIL and AUTH_PASSWORD to your preferred credentials
#   3. Run: npm run dev
#   The app will start at http://localhost:5173
# ---------------------------------------------------------------
APP_NAME=vinhsatan_internal_app
APP_ENV=production
APP_DOMAIN=app.n8nvinhsatan.site

# Frontend Vite-prefixed variables (safe to expose non-secret flags)
VITE_AUTH_MODE=single_user
# For local dev, leave VITE_API_URL empty so API calls use relative paths
VITE_API_URL=https://app.n8nvinhsatan.site

# Skip server sync in dev (set true to avoid 401 errors when no backend is running)
# VITE_NO_SERVER_SYNC=true

# Single-user credentials — vite.config.ts exposes these as VITE_AUTH_EMAIL/VITE_AUTH_PASSWORD
# (required for single_user login to work)
AUTH_EMAIL=owner@example.com
AUTH_PASSWORD=change_me

# Telegram notifications (server-side only — never use VITE_ prefix for these)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Database
DB_HOST=localhost
DB_NAME=internal_db
DB_USER=internal_user
DB_PASS=change_db_password

# Other optional variables
# VITE_NO_PERSIST=true
# ENABLE_METRICS=true

# Database connection string used by reconciler and integration tests
# Example: postgres://user:password@localhost:5432/dbname
DB_CONN=

# Temporal / Orchestrator
TEMPORAL_ADDRESS=localhost:7233
ARGO_SERVER=http://localhost:2746
ARGO_NAMESPACE=default

# IAM / KMS
# If using external KMS signer, set the URL endpoint. Otherwise, IAM_PRIVATE_KEY is used for local signing (dev only)
IAM_KMS_SIGN_URL=
IAM_PRIVATE_KEY=dev-key-change-me

# Other runtime flags
ORCHESTRATOR=temporal
