# Example environment for production/single-user deployment
# ---------------------------------------------------------------
# LOCAL DEV QUICK-START (zero backend needed):
#   cp .env.example .env
#   npm install
#   npm run dev
#   → http://localhost:5173
#   Login: admin@dev.local / dev123
# ---------------------------------------------------------------
APP_NAME=vinhsatan_internal_app
APP_ENV=development
APP_DOMAIN=localhost

# Frontend Vite-prefixed variables (safe to expose non-secret flags)
VITE_AUTH_MODE=single_user
# Leave empty for local dev — API calls use relative paths (same origin)
# Set to your deployed URL for production, e.g.: https://app.n8nvinhsatan.site
VITE_API_URL=

# Skip server sync in dev to avoid 401 noise when no backend is running
VITE_NO_SERVER_SYNC=true

# Single-user credentials — exposed to the client ONLY in single_user mode
# Change these before any real deployment
AUTH_EMAIL=admin@dev.local
AUTH_PASSWORD=dev123

# Telegram notifications (server-side only — never use VITE_ prefix for these)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Database (not required for frontend-only dev)
DB_HOST=localhost
DB_NAME=internal_db
DB_USER=internal_user
DB_PASS=change_db_password

# Other optional variables
# VITE_NO_PERSIST=true
# ENABLE_METRICS=true

# Database connection string used by reconciler and integration tests
# Example: postgres://user:password@localhost:5432/dbname
DB_CONN=

# Temporal / Orchestrator (not required for frontend-only dev)
TEMPORAL_ADDRESS=localhost:7233
ARGO_SERVER=http://localhost:2746
ARGO_NAMESPACE=default

# IAM / KMS
# If using external KMS signer, set the URL endpoint. Otherwise, IAM_PRIVATE_KEY is used for local signing (dev only)
IAM_KMS_SIGN_URL=
IAM_PRIVATE_KEY=dev-key-change-me

# Other runtime flags
ORCHESTRATOR=temporal
